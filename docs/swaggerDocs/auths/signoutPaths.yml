post:
    tags :
      - Auths
    summary: Signout to the application, remove token from user
    description: Signout to the application, remove token from user
    produces:
      - application/json
    responses:
      200:
        description: okay message
        content:
          application/json:
            schema:
              type: object
              properties:
                message: string
            example:
              message: user has logged out
      400:
        description: Incorrect content in the request.
        content:
          application/json:
            schema:
              type: object
              $ref: ../global/errorDefinition.yml
            example:
              message: Incorrect content in the request.
      500:
        description: Internal server error.
        content:
          application/json:
            schema:
              type: object
              $ref: ../global/errorDefinition.yml
            example:
              message: Internal server error.